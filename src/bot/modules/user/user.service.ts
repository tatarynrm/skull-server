// import { pool } from "../../../db/pool";

// class UserService {
//   async getUserByTelegramId(id:number) {
//     const result = await pool.query(`select * from tg users where tg_id = $1 `,[id])
//   }
// }

// export const userService = new UserService();
